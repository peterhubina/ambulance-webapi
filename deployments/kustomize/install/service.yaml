kind: Service
apiVersion: v1
metadata:
  name: peterhubina-ambulance-webapi
spec:
  selector:
    pod: peterhubina-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port